import React from 'react';

// This component is deprecated and its functionality has been moved.
// Defining it as a valid component that renders nothing to prevent errors if still referenced.
const ResultsDisplay: React.FC = () => {
  return null;
};

export default ResultsDisplay;